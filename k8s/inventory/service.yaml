apiVersion: v1
kind: Service

metadata:
  name: inventory-service
  namespace: techstore

spec:
  selector:
    app: inventory-service

  ports:
    - port: 8082
      targetPort: 8082

  type: ClusterIP